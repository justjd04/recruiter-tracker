import React from 'react';

const NoUpdate = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>No Update</h1>
    </div>
  );
};

export default NoUpdate;
